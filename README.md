csv file is dumped at a location
script runs as a scheduled task and process the contents of the csv file based on start date
this scrip assumes that the environment makes use of dynamic attributes that automatically handles license permissions and other RBAC attributes
this script also creates users remote mailbox since it was written for a hybrid environment
